{
  "name": "smapi",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "start": "npm run build && node index.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "gdserafini",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@types/module-alias": "^2.0.4",
    "@types/node": "^22.14.0",
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "module-alias": "^2.2.3"
  }
}
